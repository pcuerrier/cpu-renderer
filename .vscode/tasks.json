{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "shell",
            "label": "C/C++: CMAKE",
            "command": "cmake --build ${workspaceFolder}/build --config Debug --target all -j 10 --",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "always",
                "clear": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}